# Everything here is OPTIONAL — `docker compose up -d` works with no .env at
# all. Copy this file to .env only if you want to override a default.

# Host port the app is reachable on (the install script also writes this).
# Change it any time, then apply with: docker compose up -d
#LUMINA_PORT=7373

# Cookie-signing key. Auto-generated on first boot and persisted to
# ./data/db/session-secret if unset. Provide your own (openssl rand -hex 32)
# only if you want to manage/rotate the key yourself. Changing it logs every
# client out of every gallery.
#SESSION_SECRET=

# How many photos process (thumbnail/preview generation) concurrently in the
# background. Lower on low-core or low-RAM hardware — image processing is the
# app's main memory consumer. Raise it on a powerful box (e.g. 8) to process
# large batches faster.
#UPLOAD_CONCURRENCY=4

# Container memory ceiling. The default 2g is safe for a small NAS; raise it on
# a powerful host so higher UPLOAD_CONCURRENCY has room (e.g. 8g). Accepts
# Docker size units (m/g).
#LUMINA_MEM_LIMIT=2g

# Per-file upload size limit in bytes (default 50MB).
#MAX_UPLOAD_FILE_SIZE_BYTES=52428800

# Also generate AVIF derivatives (smaller than WebP → faster galleries on
# mobile data), served automatically to browsers that support them. Costs
# extra background encode time; set to false on slow hardware to skip.
#GENERATE_AVIF=true

# Cloudflare Tunnel token — only needed if you publish with
# `docker compose --profile tunnel up -d`. See DEPLOYMENT.md.
#CLOUDFLARE_TUNNEL_TOKEN=
